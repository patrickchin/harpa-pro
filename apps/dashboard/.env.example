VITE_API_BASE_URL=http://localhost:8787
# Optional locally; required by native Cloudflare builds. These public emails
# expose the configured password-account sign-in option, never its password.
VITE_PASSWORD_ACCOUNT_EMAILS=
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
VITE_SENTRY_RELEASE=
