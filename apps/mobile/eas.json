{
  "cli": {
    "version": ">= 18.7.0",
    "appVersionSource": "remote"
  },
  "build": {
    "_comment": "Three runtime variants. `production` ships to App Store / Play; `preview` is a side-by-side TestFlight/internal build pointed at the dev backend (com.harpa.pro.dev); `development` is the Metro dev-client local build pointed at localhost. APP_VARIANT drives bundleId + display name (see app.config.ts).",
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "channel": "development",
      "env": {
        "APP_VARIANT": "development",
        "EXPO_PUBLIC_APP_VARIANT": "development",
        "EXPO_PUBLIC_API_URL": "http://localhost:8787",
        "EXPO_PUBLIC_USE_FIXTURES": "false"
      }
    },
    "development-simulator": {
      "extends": "development",
      "ios": { "simulator": true }
    },
    "preview": {
      "distribution": "internal",
      "channel": "preview",
      "env": {
        "APP_VARIANT": "preview",
        "EXPO_PUBLIC_APP_VARIANT": "preview",
        "EXPO_PUBLIC_API_URL": "https://harpa-pro-api-dev.fly.dev",
        "EXPO_PUBLIC_USE_FIXTURES": "false"
      }
    },
    "production": {
      "channel": "production",
      "autoIncrement": true,
      "env": {
        "APP_VARIANT": "production",
        "EXPO_PUBLIC_APP_VARIANT": "production",
        "EXPO_PUBLIC_API_URL": "https://api.harpapro.com",
        "EXPO_PUBLIC_USE_FIXTURES": "false"
      }
    }
  },
  "submit": {
    "production": {}
  }
}
