/**
 * Stub for `pnpm fixtures:record <name>` — wired in P1 once a real provider
 * adapter exists. For now, just print usage.
 */
console.log('record: not yet wired — provider adapters land in P1 (see plan-p1-api-core.md)');
process.exit(2);
