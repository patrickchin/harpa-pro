# harpa-pro docs

P0.7 placeholder. Next.js app router site that will host in-app guides
and visual reference once features ship. See
`docs/legacy-v3/realignment/pages/` for the canonical per-screen specs
those guides will mirror.

```
pnpm --filter @harpa/docs dev
```
