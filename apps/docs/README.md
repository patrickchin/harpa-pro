# harpa-pro docs

P0.7 placeholder. Next.js app router site that will host in-app guides
and visual reference once features ship. Per-screen content mirrors
the shipped v4 screens in `apps/mobile`; the canonical visual
reference during the build is `../haru3-reports/apps/mobile` on
branch `dev`.

```
pnpm --filter @harpa/docs dev
```
