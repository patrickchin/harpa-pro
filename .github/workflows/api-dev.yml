name: api-dev

# Build + deploy @harpa/api to Fly.io (harpa-pro-api-dev) on every
# push to `dev`. Mirrors api-prod.yml but targets the dev app, and
# applies pending Drizzle migrations to the long-lived Neon `dev`
# branch before the Fly deploy goes out.
#
# One-time setup:
#   flyctl apps create harpa-pro-api-dev
#   pnpm db:branch:ensure dev                      # creates the Neon branch
#   doppler configs tokens create ci-github --project harpa-pro --config dev
#     → set as repo secret DOPPLER_TOKEN_DEV
# Subsequent secret changes: edit in Doppler `dev` config, push to `dev`.
# This workflow stages the synced secrets onto Fly and the `flyctl deploy`
# step activates them — secrets and code ship together.

on:
  push:
    branches: [dev]
    paths:
      - 'packages/api/**'
      - 'packages/api-contract/**'
      - 'packages/ai-fixtures/**'
      - 'infra/fly/**'
      - 'infra/neon/**'
      - 'scripts/journeys/**'
      - '.github/workflows/api-dev.yml'
      - 'pnpm-lock.yaml'
  # Allow manual re-deploys (e.g. rotating secrets without a code change).
  workflow_dispatch: {}

concurrency:
  group: api-dev
  cancel-in-progress: true # ok to cancel stale dev deploys

jobs:
  dev:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true

      - uses: ./.github/actions/setup-monorepo

      - name: Ensure Neon `dev` branch
        # Idempotent: returns the existing URI or creates the branch on
        # first run. Critical that this NOT recreate on every deploy —
        # `dev` is long-lived and holds seed data that R2 objects refer to.
        env:
          NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
          NEON_PROJECT_ID: ${{ secrets.NEON_PROJECT_ID }}
        run: |
          URI=$(pnpm --silent db:branch:ensure dev)
          echo "::add-mask::$URI"
          echo "DATABASE_URL=$URI" >> "$GITHUB_ENV"

      - name: Apply pending migrations to Neon `dev`
        env:
          DATABASE_URL: ${{ env.DATABASE_URL }}
        run: pnpm --filter @harpa/api db:migrate

      - name: Ensure admin Neon `dev` branch
        # The admin project is a separate backup/restore boundary. Its
        # long-lived dev branch is created from admin main once, then reused.
        # URI resolution is direct (not pooled) because the migrator takes a
        # session-level advisory lock.
        env:
          NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
          NEON_PROJECT_ID: ${{ vars.NEON_ADMIN_PROJECT_ID }}
          NEON_DATABASE_NAME: harpa_admin
          NEON_ROLE_NAME: harpa_admin_owner
        run: |
          URI=$(pnpm --silent db:branch:ensure dev main)
          echo "::add-mask::$URI"
          echo "ADMIN_DATABASE_URL=$URI" >> "$GITHUB_ENV"

      - name: Apply pending admin migrations to Neon `dev`
        env:
          ADMIN_DATABASE_URL: ${{ env.ADMIN_DATABASE_URL }}
        run: pnpm --filter @harpa/api db:migrate:admin

      - uses: superfly/flyctl-actions/setup-flyctl@1.5

      - uses: dopplerhq/cli-action@v3

      - name: Sync Fly secrets from Doppler
        # Pulls the `dev` config and stages it onto the Fly app. Filters
        # out vars that don't belong on Fly (Doppler/Neon/CF/build-time
        # PUBLIC_* / EXPO_PUBLIC_*). `--stage` defers activation; the
        # subsequent `flyctl deploy` is what flips them on, so secrets
        # + code ship in a single transaction.
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN_DEV }}
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          set -euo pipefail
          FLY_SECRET_LIST=$(flyctl secrets list --app harpa-pro-api-dev)
          if printf '%s\n' "$FLY_SECRET_LIST" \
            | awk 'NR > 1 { print $1 }' \
            | grep -qx ADMIN_CORS_ORIGINS; then
            flyctl secrets unset ADMIN_CORS_ORIGINS \
              --stage --app harpa-pro-api-dev
          fi
          {
            doppler secrets download --no-file --format env \
              | grep -vE '^(DOPPLER_|NEON_|FLY_|CLOUDFLARE_|PUBLIC_|EXPO_PUBLIC_|PAGES_PROJECT|PORT|NODE_ENV|ADMIN_EMAIL|ADMIN_DATABASE_URL=|ADMIN_CORS_ORIGINS=|BETTER_AUTH_URL=)'
            echo "ADMIN_DATABASE_URL=$ADMIN_DATABASE_URL"
            echo "BETTER_AUTH_URL=https://harpa-pro-api-dev.fly.dev"
          } | flyctl secrets import --stage --app harpa-pro-api-dev

      - name: Deploy to Fly.io (dev)
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          set -euo pipefail
          MIGRATION_FILES=(packages/api/migrations/*.sql)
          if [[ ! -e "${MIGRATION_FILES[0]}" ]]; then
            echo "ERROR: no .sql files in packages/api/migrations" >&2
            exit 1
          fi
          HEAD=$(printf '%s\n' "${MIGRATION_FILES[@]##*/}" | LC_ALL=C sort | tail -1)
          echo "deploy: MIGRATIONS_REQUIRED_HEAD=$HEAD"
          ADMIN_MIGRATION_FILES=(packages/api/admin-migrations/*.sql)
          if [[ ! -e "${ADMIN_MIGRATION_FILES[0]}" ]]; then
            echo "ERROR: no .sql files in packages/api/admin-migrations" >&2
            exit 1
          fi
          ADMIN_HEAD=$(printf '%s\n' "${ADMIN_MIGRATION_FILES[@]##*/}" | LC_ALL=C sort | tail -1)
          echo "deploy: ADMIN_MIGRATIONS_REQUIRED_HEAD=$ADMIN_HEAD"
          GIT_COMMIT=$(git rev-parse HEAD)
          BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          echo "deploy: GIT_COMMIT=$GIT_COMMIT BUILD_TIME=$BUILD_TIME"
          flyctl deploy \
            --config infra/fly/fly.dev.toml \
            --dockerfile infra/fly/Dockerfile \
            --build-arg "MIGRATIONS_REQUIRED_HEAD=$HEAD" \
            --build-arg "ADMIN_MIGRATIONS_REQUIRED_HEAD=$ADMIN_HEAD" \
            --build-arg "GIT_COMMIT=$GIT_COMMIT" \
            --build-arg "BUILD_TIME=$BUILD_TIME" \
            --remote-only
          bash scripts/ci/repair-storage-worker-topology.sh harpa-pro-api-dev
          bash scripts/ci/verify-storage-worker-started.sh harpa-pro-api-dev
          STORAGE_LEASE_ROLLOUT_GRACE_SEC=330 \
            pnpm --filter @harpa/api storage:arm-leases

      - name: Verify /readyz (dev)
        # Dev runs with `auto_stop_machines = "suspend"` and
        # `min_machines_running = 0`, so machines are stopped right
        # after a deploy and the first request has to wake them. The
        # cold-start (Linux + Node + DB pool + schema-head check)
        # routinely exceeds the old 5s curl budget — see
        # docs/bugs/README.md ("api-dev /readyz verify cold-start").
        # The shared script bumps the per-attempt timeout and retry
        # spacing so cold-starts pass without flakes.
        env:
          READYZ_URL: ${{ vars.API_READY_URL_DEV || 'https://harpa-pro-api-dev.fly.dev/readyz' }}
        run: bash scripts/ci/verify-readyz.sh

      - name: Verify /admin/readyz (dev)
        # This is deliberately post-deploy rather than a Fly machine health
        # check: an admin DB incident must not take the app API out of service.
        env:
          READYZ_URL: https://harpa-pro-api-dev.fly.dev/admin/readyz
        run: bash scripts/ci/verify-readyz.sh

      - name: Diagnose readyz failure
        # Surface Fly machine state + recent logs in the workflow log
        # so the next regression doesn't require a separate spelunk
        # through the Fly dashboard. Only runs if /readyz didn't pass.
        if: failure()
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          echo "::group::flyctl status"
          flyctl status --app harpa-pro-api-dev || true
          echo "::endgroup::"
          echo "::group::flyctl logs (last 200 lines)"
          flyctl logs --app harpa-pro-api-dev --no-tail || true
          echo "::endgroup::"

      - name: Post-deploy journey tests
        # Runs curl journey scripts against the just-deployed dev API.
        # Order: stress (~10s, no AI) → core (~3min, live AI) → extended (~5min, live AI).
        # On failure the workflow goes red but the deploy is NOT rolled back.
        #
        # EMAIL / EMAIL2 come from GitHub repo *variables* (not secrets —
        # the addresses themselves aren't sensitive). They must match
        # entries in Fly's TEST_ACCOUNT_EMAILS_DEV secret (and are
        # therefore the addresses `db:seed-test-account` provisions during
        # release_command). If EMAIL2 isn't a real second account, set
        # it to the same value as EMAIL — extended/stress gracefully skip
        # second-user checks when only one user exists. See
        # docs/bugs/2026-06-06-test-accounts-never-seeded-on-dev.md.
        env:
          PASSWORD: ${{ secrets.TEST_ACCOUNT_PASSWORD_DEV }}
          EMAIL: ${{ vars.TEST_ACCOUNT_EMAIL_DEV }}
          EMAIL2: ${{ vars.TEST_ACCOUNT_EMAIL2_DEV }}
        run: |
          if [[ -z "${EMAIL:-}" ]]; then
            echo "::error::TEST_ACCOUNT_EMAIL_DEV repo variable is unset; cannot run journeys" >&2
            exit 1
          fi
          bash scripts/journeys/all.sh dev
  mobile-ota:
    name: Publish API-dependent mobile OTA
    needs: dev
    permissions:
      contents: write
    uses: ./.github/workflows/mobile-ota-dev.yml
    with:
      release_sha: ${{ github.sha }}
      api_deploy_succeeded: true
    secrets: inherit
